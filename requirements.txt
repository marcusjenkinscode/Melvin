# Melvin – Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# On Termux (Android) the same command works after running setup.sh.

# HTTP client for the Ollama REST API
requests>=2.28.0

# Symmetric encryption (AES-128-CTR / Fernet)
cryptography>=41.0.0
